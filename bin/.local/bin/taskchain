#!/bin/bash

# Usage: $ taskchain 100 101 102 103 104

echo "$# arguments"
argv=("$@")

for (( i=$#-1; i > 0; i-- ))
do
  echo task ${argv[i]} modify depends:${argv[i-1]}
  task ${argv[i]} modify depends:${argv[i-1]}
done
